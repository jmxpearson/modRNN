# Cognitax Project Memory

## Project Overview
- Uses `uv` for env management, `uv run pytest test/` for tests, `uv run ruff check/format`
- Add dependencies via `uv` rather than modifying `pyproject.toml` directly.

